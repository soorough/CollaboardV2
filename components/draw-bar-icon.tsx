import type { NextPage } from "next";

const DrawBarIcon: NextPage = () => {
  return (
    <img
      className="w-full h-full z-[2] object-contain absolute left-[-15px] top-[0px] [transform:scale(1.037)]"
      loading="lazy"
      alt=""
      src="/drawbar.svg"
    />
  );
};

export default DrawBarIcon;
